rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Liverton stores uploaded media in Cloudinary through the authenticated
    // server-signing endpoint. Firebase Storage is intentionally disabled so
    // files cannot bypass Cloudinary authorization.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
