[1mdiff --git a/src/contexts/AuthContext.tsx b/src/contexts/AuthContext.tsx[m
[1mindex db534e1..ae7889b 100644[m
[1m--- a/src/contexts/AuthContext.tsx[m
[1m+++ b/src/contexts/AuthContext.tsx[m
[36m@@ -92,6 +92,9 @@[m [mexport function AuthProvider({ children }: { children: ReactNode }) {[m
       console.warn('Unable to sync searchable user identity:', error);[m
     }[m
 [m
[32m+[m[32m    // Notifications are a convenience side effect. They must never block[m
[32m+[m[32m    // authentication or profile persistence when rules are stale or a reminder[m
[32m+[m[32m    // document is unavailable.[m
     if (setupStatus.percentage < 100) {[m
       void createAccountSetupReminder(profile, user, profile.role)[m
         .catch((error) => console.warn('Unable to create account setup reminder:', error));[m
[36m@@ -442,12 +445,21 @@[m [mexport function AuthProvider({ children }: { children: ReactNode }) {[m
       try {[m
         await syncAccountIdentity(nextProfile, currentUser);[m
       } catch (error) {[m
[32m+[m[32m        // The primary profile write already succeeded. Directory indexing is a[m
[32m+[m[32m        // secondary convenience and must not make the save look unsuccessful.[m
         console.warn('Unable to refresh searchable user identity:', error);[m
       }[m
       if (claimedUsername && previousUsername && previousUsername !== claimedUsername) {[m
[31m-        await releaseUsername(previousUsername, currentUser.uid);[m
[32m+[m[32m        await releaseUsername(previousUsername, currentUser.uid).catch((error) => {[m
[32m+[m[32m          console.warn('Unable to release previous username claim:', error);[m
[32m+[m[32m        });[m
       }[m
[31m-      await syncAccountOnOpen(nextProfile, currentUser);[m
[32m+[m[32m      // Account-setup reminders are also secondary side effects. A stale or[m
[32m+[m[32m      // partially deployed ruleset must not make profile editing fail after the[m
[32m+[m[32m      // users/{uid} and role profile documents have been written.[m
[32m+[m[32m      await syncAccountOnOpen(nextProfile, currentUser).catch((error) => {[m
[32m+[m[32m        console.warn('Unable to refresh account setup side effects:', error);[m
[32m+[m[32m      });[m
     }[m
   };[m
 [m
[1mdiff --git a/src/lib/documents.ts b/src/lib/documents.ts[m
[1mindex 3d8cb3d..2b9a0b6 100644[m
[1m--- a/src/lib/documents.ts[m
[1m+++ b/src/lib/documents.ts[m
[36m@@ -16,13 +16,8 @@[m [mimport {[m
   type DocumentData,[m
   type QueryDocumentSnapshot[m
 } from 'firebase/firestore';[m
[31m-import {[m
[31m-  getDownloadURL,[m
[31m-  ref,[m
[31m-  uploadBytes,[m
[31m-  deleteObject,[m
[31m-} from 'firebase/storage';[m
[31m-import { db, storage } from '@/lib/firebase';[m
[32m+[m[32mimport { db } from '@/lib/firebase';[m
[32m+[m[32mimport { uploadToCloudinary } from '@/services/cloudinaryService';[m
 import { toDate } from '@/lib/date';[m
 import type {[m
   DocumentContent,[m
[36m@@ -474,9 +469,10 @@[m [mexport async function uploadFileToDocument(params: {[m
   docId: string;[m
   file: File;[m
 }): Promise<string> {[m
[31m-  const objectRef = ref(storage, `documents/${params.docId}/${params.file.name}`);[m
[31m-  await uploadBytes(objectRef, params.file);[m
[31m-  const url = await getDownloadURL(objectRef);[m
[32m+[m[32m  const url = await uploadToCloudinary(params.file, 'document', {[m
[32m+[m[32m    referenceId: params.docId,[m
[32m+[m[32m    purpose: 'document_attachment',[m
[32m+[m[32m  });[m
   await updateDoc(doc(db, DOCUMENTS_COLLECTION, params.docId), {[m
     fileUrl: url,[m
     updatedAt: serverTimestamp(),[m
[36m@@ -485,8 +481,9 @@[m [mexport async function uploadFileToDocument(params: {[m
 }[m
 [m
 export async function deleteDocumentFile(params: { docId: string; filePath: string }): Promise<void> {[m
[31m-  const objectRef = ref(storage, params.filePath);[m
[31m-  await deleteObject(objectRef);[m
[32m+[m[32m  // Cloudinary assets are managed outside Firebase Storage. Remove the[m
[32m+[m[32m  // document reference here; Cloudinary deletion requires a secured server[m
[32m+[m[32m  // destroy endpoint and is not attempted from the client.[m
   await updateDoc(doc(db, DOCUMENTS_COLLECTION, params.docId), {[m
     fileUrl: null,[m
     updatedAt: serverTimestamp(),[m
[1mdiff --git a/src/pages/admin/DashboardAnnouncementManagement.tsx b/src/pages/admin/DashboardAnnouncementManagement.tsx[m
[1mindex 10f812a..fffedf7 100644[m
[1m--- a/src/pages/admin/DashboardAnnouncementManagement.tsx[m
[1m+++ b/src/pages/admin/DashboardAnnouncementManagement.tsx[m
[36m@@ -39,7 +39,7 @@[m [mimport {[m
   serverTimestamp,[m
   Timestamp,[m
 } from 'firebase/firestore';[m
[31m-import { getStorage, ref, uploadBytes, getDownloadURL } from 'firebase/storage';[m
[32m+[m[32mimport { uploadToCloudinary } from '@/services/cloudinaryService';[m
 import type { DashboardAnnouncement, AnnouncementType } from '@/types/announcement';[m
 [m
 export default function DashboardAnnouncementManagement() {[m
[36m@@ -156,15 +156,14 @@[m [mexport default function DashboardAnnouncementManagement() {[m
     setVideoFile(file);[m
   };[m
 [m
[31m-  // Upload file to Firebase Storage[m
[31m-  const uploadFile = async (file: File, path: string): Promise<string> => {[m
[31m-    const storage = getStorage();[m
[31m-    const fileRef = ref(storage, `announcements/${path}/${Date.now()}_${file.name}`);[m
[31m-    [m
[31m-    const snapshot = await uploadBytes(fileRef, file);[m
[31m-    const downloadUrl = await getDownloadURL(snapshot.ref);[m
[31m-    [m
[31m-    return downloadUrl;[m
[32m+[m[32m  // Upload announcement media through the authenticated signed Cloudinary flow.[m
[32m+[m[32m  const uploadFile = async (file: File): Promise<string> => {[m
[32m+[m[32m    const type = file.type.startsWith('video/') ? 'course_video' : 'image';[m
[32m+[m[32m    return uploadToCloudinary(file, type, {[m
[32m+[m[32m      onProgress: (percent) => setUploadProgress(Math.max(30, Math.min(70, percent))),[m
[32m+[m[32m      showErrorToast: false,[m
[32m+[m[32m      purpose: 'dashboard_announcement',[m
[32m+[m[32m    });[m
   };[m
 [m
   // Handle form submission[m
[36m@@ -201,13 +200,13 @@[m [mexport default function DashboardAnnouncementManagement() {[m
       // Upload files[m
       if (announcementType === 'image' && imageFile) {[m
         setUploadProgress(30);[m
[31m-        imageUrl = await uploadFile(imageFile, 'images');[m
[32m+[m[32m        imageUrl = await uploadFile(imageFile);[m
         setUploadProgress(60);[m
       }[m
 [m
       if (announcementType === 'video' && videoFile) {[m
         setUploadProgress(30);[m
[31m-        videoUrl = await uploadFile(videoFile, 'videos');[m
[32m+[m[32m        videoUrl = await uploadFile(videoFile);[m
         setUploadProgress(60);[m
       }[m
 [m
[1mdiff --git a/src/pages/features/DocumentEditor.tsx b/src/pages/features/DocumentEditor.tsx[m
[1mindex f1356a2..51755fe 100644[m
[1m--- a/src/pages/features/DocumentEditor.tsx[m
[1m+++ b/src/pages/features/DocumentEditor.tsx[m
[36m@@ -143,30 +143,50 @@[m [mexport default function DocumentEditor() {[m
     fetchMeta();[m
   }, [docId, currentUser]);[m
 [m
[31m-  // 2. Dynamically Load PDF.js from CDN[m
[32m+[m[32m  // 2. Dynamically load PDF.js from CDN. If the CDN or worker is[m
[32m+[m[32m  // unavailable, the reader falls back to the browser's native PDF viewer.[m
   useEffect(() => {[m
[31m-    const initPDFJS = async () => {[m
[31m-      try {[m
[31m-        if (docMeta && !isPdfDocumentRecord(docMeta)) return;[m
[31m-        if ((window as any).pdfjsLib) {[m
[31m-          setPdfjs((window as any).pdfjsLib);[m
[31m-          return;[m
[31m-        }[m
[31m-        const script = document.createElement('script');[m
[31m-        script.src = PDFJS_SCRIPT_URL;[m
[31m-        script.onload = () => {[m
[31m-          const lib = (window as any).pdfjsLib;[m
[31m-          lib.GlobalWorkerOptions.workerSrc = PDFJS_WORKER_URL;[m
[31m-          setPdfjs(lib);[m
[31m-        };[m
[31m-        document.head.appendChild(script);[m
[31m-      } catch (err) {[m
[31m-        console.error('Failed to load PDF.js script:', err);[m
[31m-        setPdfError('Failed to load browser PDF renderer script.');[m
[32m+[m[32m    if (docMeta && !isPdfDocumentRecord(docMeta)) return;[m
[32m+[m[32m    if ((window as any).pdfjsLib) {[m
[32m+[m[32m      const lib = (window as any).pdfjsLib;[m
[32m+[m[32m      lib.GlobalWorkerOptions.workerSrc = PDFJS_WORKER_URL;[m
[32m+[m[32m      setPdfjs(lib);[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    const script = document.createElement('script');[m
[32m+[m[32m    let timeoutId: number | undefined;[m
[32m+[m[32m    const fail = (message: string) => {[m
[32m+[m[32m      console.error(message);[m
[32m+[m[32m      setPdfError(message);[m
[32m+[m[32m    };[m
[32m+[m[32m    script.src = PDFJS_SCRIPT_URL;[m
[32m+[m[32m    script.async = true;[m
[32m+[m[32m    script.onload = () => {[m
[32m+[m[32m      if (timeoutId) window.clearTimeout(timeoutId);[m
[32m+[m[32m      const lib = (window as any).pdfjsLib;[m
[32m+[m[32m      if (!lib) {[m
[32m+[m[32m        fail('PDF renderer loaded without its browser API.');[m
[32m+[m[32m        return;[m
       }[m
[32m+[m[32m      lib.GlobalWorkerOptions.workerSrc = PDFJS_WORKER_URL;[m
[32m+[m[32m      setPdfjs(lib);[m
     };[m
[31m-    initPDFJS();[m
[31m-  }, []);[m
[32m+[m[32m    script.onerror = () => {[m
[32m+[m[32m      if (timeoutId) window.clearTimeout(timeoutId);[m
[32m+[m[32m      fail('The enhanced PDF renderer could not be downloaded.');[m
[32m+[m[32m    };[m
[32m+[m[32m    timeoutId = window.setTimeout(() => {[m
[32m+[m[32m      script.remove();[m
[32m+[m[32m      fail('The enhanced PDF renderer timed out while loading.');[m
[32m+[m[32m    }, 10000);[m
[32m+[m[32m    document.head.appendChild(script);[m
[32m+[m
[32m+[m[32m    return () => {[m
[32m+[m[32m      if (timeoutId) window.clearTimeout(timeoutId);[m
[32m+[m[32m      script.remove();[m
[32m+[m[32m    };[m
[32m+[m[32m  }, [docMeta]);[m
 [m
   // 3. Load PDF Document once Metadata and PDF.js are loaded[m
   useEffect(() => {[m
[36m@@ -180,11 +200,17 @@[m [mexport default function DocumentEditor() {[m
         setPdfDoc(docInstance);[m
         setNumPages(docInstance.numPages);[m
 [m
[31m-        // Update Page Count in metadata if it is 0[m
[32m+[m[32m        // Page-count metadata is optional. Do not let a stale ruleset or a[m
[32m+[m[32m        // shared-document write restriction make a successfully loaded PDF[m
[32m+[m[32m        // appear unreadable.[m
         if (!docMeta.pageCount || docMeta.pageCount === 0) {[m
[31m-          const refDoc = doc(db, 'documents', docId!);[m
[31m-          await updateDoc(refDoc, { pageCount: docInstance.numPages });[m
[31m-          setDocMeta((prev: any) => ({ ...prev, pageCount: docInstance.numPages }));[m
[32m+[m[32m          try {[m
[32m+[m[32m            const refDoc = doc(db, 'documents', docId!);[m
[32m+[m[32m            await updateDoc(refDoc, { pageCount: docInstance.numPages });[m
[32m+[m[32m            setDocMeta((prev: any) => ({ ...prev, pageCount: docInstance.numPages }));[m
[32m+[m[32m          } catch (metadataError) {[m
[32m+[m[32m            console.warn('Unable to persist PDF page count:', metadataError);[m
[32m+[m[32m          }[m
         }[m
 [m
         // Fetch first page aspect ratio[m
[36m@@ -874,15 +900,20 @@[m [mexport default function DocumentEditor() {[m
                 <p className="text-sm text-slate-500 font-bold animate-pulse">Rendering high-quality PDF canvases...</p>[m
               </div>[m
             ) : pdfError ? ([m
[31m-              <div className="flex-1 flex flex-col items-center justify-center p-6 text-center max-w-md mx-auto space-y-4">[m
[31m-                <HelpCircle className="w-16 h-16 text-red-500" />[m
[31m-                <div className="space-y-1">[m
[31m-                  <h2 className="font-extrabold text-lg text-slate-800 dark:text-white">Failed to load PDF</h2>[m
[31m-                  <p className="text-xs text-slate-400 leading-relaxed">{pdfError}</p>[m
[32m+[m[32m              <div className="flex-1 flex flex-col min-h-0 p-3 sm:p-5 gap-3">[m
[32m+[m[32m                <div className="flex items-center justify-between gap-3 rounded-xl border border-amber-200 bg-amber-50 px-3 py-2 text-amber-900 dark:border-amber-900/50 dark:bg-amber-950/20 dark:text-amber-200">[m
[32m+[m[32m                  <p className="text-xs leading-relaxed">The enhanced reader could not start. You can still preview the original PDF below.</p>[m
[32m+[m[32m                  <Button variant="outline" size="sm" onClick={() => navigate('/dashboard/documents')} className="shrink-0">Back</Button>[m
                 </div>[m
[31m-                <Button variant="outline" onClick={() => navigate('/dashboard/documents')} className="glass-card font-bold">[m
[31m-                  Return to library[m
[31m-                </Button>[m
[32m+[m[32m                {docMeta?.fileUrl ? ([m
[32m+[m[32m                  <iframe[m
[32m+[m[32m                    src={`${docMeta.fileUrl}#toolbar=1&view=FitH`}[m
[32m+[m[32m                    title={`${docMeta.title || 'PDF'} preview`}[m
[32m+[m[32m                    className="min-h-0 flex-1 w-full rounded-xl border border-slate-200 bg-white dark:border-slate-800"[m
[32m+[m[32m                  />[m
[32m+[m[32m                ) : ([m
[32m+[m[32m                  <div className="flex-1 flex items-center justify-center text-sm text-slate-500">No PDF file URL is available.</div>[m
[32m+[m[32m                )}[m
               </div>[m
             ) : ([m
               <div[m
[1mdiff --git a/src/pages/features/ProfileSystem.tsx b/src/pages/features/ProfileSystem.tsx[m
[1mindex 62b1cdd..b508e41 100644[m
[1m--- a/src/pages/features/ProfileSystem.tsx[m
[1m+++ b/src/pages/features/ProfileSystem.tsx[m
[36m@@ -1,8 +1,8 @@[m
 import { useState, useEffect } from 'react';[m
 import { useAuth } from '../../contexts/AuthContext';[m
[31m-import { db, storage } from '../../lib/firebase';[m
[32m+[m[32mimport { db } from '../../lib/firebase';[m
 import { doc, getDoc, updateDoc, setDoc, Timestamp } from 'firebase/firestore';[m
[31m-import { ref, uploadBytes, getDownloadURL } from 'firebase/storage';[m
[32m+[m[32mimport { uploadToCloudinary } from '../../services/cloudinaryService';[m
 import { Button } from '../../components/ui/button';[m
 import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../../components/ui/card';[m
 import { Input } from '../../components/ui/input';[m
[36m@@ -160,9 +160,11 @@[m [mexport default function ProfileSystem() {[m
 [m
     try {[m
       setUploadingPhoto(true);[m
[31m-      const storageRef = ref(storage, `profiles/${currentUser.uid}/photo`);[m
[31m-      await uploadBytes(storageRef, photoFile);[m
[31m-      const photoURL = await getDownloadURL(storageRef);[m
[32m+[m[32m      const photoURL = await uploadToCloudinary(photoFile, 'image', {[m
[32m+[m[32m        showErrorToast: false,[m
[32m+[m[32m        userId: currentUser.uid,[m
[32m+[m[32m        purpose: 'profile_picture',[m
[32m+[m[32m      });[m
 [m
       await updateDoc(doc(db, 'profiles', currentUser.uid), {[m
         photoURL,[m
[1mdiff --git a/src/services/courseService.ts b/src/services/courseService.ts[m
[1mindex 2af08b9..ea6d54d 100644[m
[1m--- a/src/services/courseService.ts[m
[1m+++ b/src/services/courseService.ts[m
[36m@@ -18,14 +18,11 @@[m [mimport {[m
   arrayUnion,[m
   serverTimestamp[m
 } from 'firebase/firestore';[m
[31m-import { [m
[31m-  ref, [m
[31m-  deleteObject [m
[31m-} from 'firebase/storage';[m
[32m+[m
 import type { Unsubscribe } from 'firebase/firestore';[m
 import { uploadToCloudinary } from './cloudinaryService';[m
 import { dispatchEnrollmentNotification } from './notificationService';[m
[31m-import { db, storage } from '@/lib/firebase';[m
[32m+[m[32mimport { db } from '@/lib/firebase';[m
 [m
 // ==========================================[m
 // TYPES[m
[36m@@ -204,14 +201,9 @@[m [mexport async function deleteCourseMaterial(courseId: string, materialId: string)[m
   // First remove from Firestore[m
   await removeCourseMaterial(courseId, materialId);[m
   [m
[31m-  // Then delete from storage[m
[31m-  try {[m
[31m-    const storageRef = ref(storage, `courses/${courseId}/materials/${materialId}`);[m
[31m-    await deleteObject(storageRef);[m
[31m-  } catch (error) {[m
[31m-    console.error('Error deleting file from storage:', error);[m
[31m-    // Continue even if storage deletion fails[m
[31m-  }[m
[32m+[m[32m  // Course files are stored in Cloudinary. Firestore metadata is removed[m
[32m+[m[32m  // above; Cloudinary deletion requires a secured server-side destroy call.[m
[32m+[m[32m  // The old client-side Firebase Storage deletion has been removed.[m
 }[m
 [m
 // ==========================================[m
[1mdiff --git a/storage.rules b/storage.rules[m
[1mindex 268a466..5450307 100644[m
[1m--- a/storage.rules[m
[1m+++ b/storage.rules[m
[36m@@ -1,45 +1,12 @@[m
 rules_version = '2';[m
[32m+[m
 service firebase.storage {[m
   match /b/{bucket}/o {[m
[31m-[m
[31m-    // ── Banner media: Platform admins can upload; all authenticated users can read ──[m
[31m-    match /banners/{allPaths=**} {[m
[31m-      // Any authenticated user can READ banner images/videos[m
[31m-      allow read: if request.auth != null;[m
[31m-[m
[31m-      // Only authenticated users can WRITE (upload) – platform admin check[m
[31m-      // is enforced at the app level, but we still require auth here[m
[31m-      allow write: if request.auth != null;[m
[31m-    }[m
[31m-[m
[31m-    // ── User profile pictures ──[m
[31m-    match /users/{userId}/{allPaths=**} {[m
[31m-      allow read: if request.auth != null;[m
[31m-      allow write: if request.auth != null && request.auth.uid == userId;[m
[31m-    }[m
[31m-[m
[31m-    // ── School assets ──[m
[31m-    match /schools/{allPaths=**} {[m
[31m-      allow read: if request.auth != null;[m
[31m-      allow write: if request.auth != null;[m
[31m-    }[m
[31m-[m
[31m-    // ── Course materials ──[m
[31m-    match /courses/{allPaths=**} {[m
[31m-      allow read: if request.auth != null;[m
[31m-      allow write: if request.auth != null;[m
[31m-    }[m
[31m-[m
[31m-    // ── Documents ──[m
[31m-    match /documents/{allPaths=**} {[m
[31m-      allow read: if request.auth != null;[m
[31m-      allow write: if request.auth != null;[m
[31m-    }[m
[31m-[m
[31m-    // ── Default: authenticated users can read; deny write ──[m
[32m+[m[32m    // Liverton stores uploaded media in Cloudinary through the authenticated[m
[32m+[m[32m    // server-signing endpoint. Firebase Storage is intentionally disabled so[m
[32m+[m[32m    // files cannot bypass Cloudinary authorization.[m
     match /{allPaths=**} {[m
[31m-      allow read: if request.auth != null;[m
[31m-      allow write: if request.auth != null;[m
[32m+[m[32m      allow read, write: if false;[m
     }[m
   }[m
 }[m
